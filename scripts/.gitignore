# Build tooling artifacts — the compiled .mjs files are committed to the
# skill's dist/ by `make`, so end users don't need to install or build.
node_modules/
package-lock.json

# Generated Aura REST client (regenerate with `make codegen`).
src/generated/
# Transient openapi-ts error logs.
openapi-ts-error-*.log
