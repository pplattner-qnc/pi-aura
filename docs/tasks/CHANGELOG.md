# Task Changelog

## 2026-08-24 — Rewrite the aura-digest SKILL.md flow for the interactive dashboard (skl-flow-rewrite)
Rewrote the `aura-digest` SKILL.md **Step 4** from a one-way markdown dump to the **interactive dashboard choreography** (prose-only; the contract the agent follows). The new flow: `save` → (fetch already wrote `~/.pi/aura/digest.json`) → `cleanup` → **start the dashboard** (`digest-dashboard-start` tool or `/digest-dashboard start`) → **wait for a click** (the listener forwards `action_click` as a custom message that wakes a new turn; don't poll/prompt) → **act on exactly one action** (load the `aura` skill, route on `action.aura_use_case`, set `followup.currentlyWorkingOn` before acting, act, write an `ack` event to `state.json` + clear `currentlyWorkingOn` after, report) → return to wait, or **clean close** (one-line verdict + `/digest-dashboard stop` + stop, no dangling prompt). Added a **"Routing table"** reference section (section → action → `aura_use_case`, mirroring `buildActions` as source of truth), an **"Agent-side writes"** subsection with the exact `node -e` one-liners for the lock + ack/clear (validated against temp files), and updated the **Scope and handoff** (handoff triggered by a forwarded click, route through `aura`, teardown is `/digest-dashboard stop`). Removed the `[ASK]` block (the dashboard's buttons are the ask). Steps 1–3 + the markdown path unchanged. Three `hitl` slices landed sequentially. Outcome: the `aura-digest` skill is now the complete interactive contract tying the `digest-actions-and-followup` data + the `digest-dashboard` mechanism together.

## 2026-08-24 — Build the digest-dashboard extension sub-package (digest-dashboard)
Built the **whole `.pi/extensions/digest-dashboard/` sub-package** — the interactive surface half of the dashboard. A **Svelte 5 SPA** (`Digest.svelte`, Vite lib/iife → committed `dist/app.js`+`app.css`) renders `~/.pi/aura/digest.json` (sections + an action button per `actions[]`, with a `followup.currentlyWorkingOn` spinner + "Working…" tag + disabled siblings + SSE hot-reload + click→`POST /api/state`). A **dumb file server** (`server.ts`, esbuild → committed `dist/server.mjs`) serves the static shell + `/api/digest` + SSE `/events` + `POST /api/state` + writes `server-url.json` on listen. A **bidirectional append-only `state.json`** (`state.ts` `StateFile`+helpers) carries `page→agent` clicks + `agent→page` acks + the server PID. A **pi extension** (`index.ts`) spawns the detached server (`spawn({detached:true,stdio:'ignore'})`+`unref()`, PID in `state.json`), runs an **in-process `fs.watch` listener** that forwards `action_click` events via `pi.sendMessage({triggerTurn})`, and provides `/digest-dashboard start|stop` + a `digest-dashboard-start` tool. Six slices (skeleton → SPA → server → listener → teardown → wire-entry) landed sequentially via TDD; **42 vitest tests** (happy-dom) + typecheck + build green. Mirror `pi-annotate`'s zero-runtime-deps discipline (devDeps at root, committed `dist/`, marker sub-pkg `package.json`). Followed by an **Impeccable critique + polish pass**: Assessment A (design review, heuristic scoring) + Assessment B (detector, zero source findings; URL scan blocked by missing puppeteer) → applied P0+P1 fixes (priorities-to-top, filled primary button, visible "Working…" tag, section hierarchy tiers, capacity metric row + utilization bar, queue table responsive + a11y, design tokens, `:focus-visible` + disabled-contrast + `prefers-reduced-motion`, positive empty-states + all-clear banner); wrote `DESIGN.md`. Outcome: the interactive dashboard mechanism + UI is complete and ready for the `skl-flow-rewrite` choreography.

## 2026-08-24 — Compute the digest actions[] routing table + followup + write ~/.pi/aura/digest.json (digest-actions-and-followup)
Added the **data half** of the interactive digest dashboard: a structured `actions[]` routing table (`DigestAction = {section,key,action,label,instruction,aura_use_case}`) computed by `buildActions()` in a new `scripts/src/build-actions.ts` (replaces `seedSuggestedActions`; `suggested_actions` is now derived from `actions[].instruction` — single ranking, ≤6, stale corrections dropped), plus a `followup.currentlyWorkingOn` in-flight lock (default `null`) and a new `~/.pi/aura/digest.json` write target (`DASHBOARD_DIGEST_PATH` + `writeDashboardDigest()` helper, non-fatal on write failure). Three slices (actions-routing-table, followup-working-on, digest-json-writer) landed sequentially via TDD; 13 unit tests (`build-actions` 8 + `followup` 1 + `write-dashboard-digest` 3 + regressions), typecheck + build + shared pkg 30/30 green. The markdown render path (`render`/`save`/`diff`/`last`/`cleanup`) is unchanged. Outcome: the SPA data source (`~/.pi/aura/digest.json`) and the routing table are ready for the `digest-dashboard` task to render.

## 2026-08-21 — Salvage aura-mcp-overhaul docs onto main + re-implement the review CLI on AuraClient (aura-mcp-doc-salvage)
Salvaged the doc/audit half of the `aura-mcp-overhaul-update` branch (cut from stale v0.3.0; its code duplicated main's `aura-access-rewrite`) onto current main: cleaned ~30 gone-MCP-tool references (195→90 overhaul) across 16 skill-resource files to zero (or explicit "not available via MCP/REST" notes) + documented 21 newly-exposed tools (tags, phase goals, neighborhood, discard/reopen, projects, people-finding, comment-update, spaces admin, Asana read, glossary), and added the `replacement-table.md` audit. Then `openapi-spec-bump` replaced `packages/shared/openapi/openapi.yaml` with the user-provided 2026-08-21 spec, regenerated codegen (the new spec was purely additive; no existing shapes changed), and **fixed main's pre-existing red typecheck** (missing `generated/sdk.gen.js` + an implicit-any in `hey-api-aura-client.ts:695`). Then `aura-review-subcommands` added 6 review/approval verbs to `AuraClient`/`HeyApiAuraClient` (with `mock.method`-based unit tests) + wired the 6 `aura.mjs artifact review-*` subcommands via `createDefaultAuraClient()`, making the salvaged doc references truthful; `artifact-management.md` updated so `review-reopen` shows its required `--version` flag (the generated SDK needs it). Outcome: main's skill docs are consistent with the live 90-tool MCP surface, the REST spec is current, and the artifact-review flow is agent-driveable via `aura.mjs`.

## 2026-08-20 — Remove the Aura bearerClient path from clients.ts (clients-cleanup)
Removed the dead Aura MCP-over-HTTP path: `bearerClient` from `scripts/src/clients.ts` + the dead `scripts/src/aura-client.ts` re-export shim + the dead scripts codegen tree (`scripts/src/generated/`, `scripts/openapi/`, `scripts/openapi-ts.config.ts`) + the `@hey-api/client-fetch`/`@hey-api/openapi-ts` deps and `codegen` script from `scripts/package.json` + dead `scripts/.gitignore` entries. Kept the Atlassian path (`atlassianClient`, `readOAuthTokenFromKeyring`, `loadMcpConfig`, the keyring chunk logic) and `mcp-client.ts` untouched, and kept `@napi-rs/keyring` in `scripts/package.json` (the Atlassian keyring-read still needs it — the workspaces grilling's "drop @napi-rs/keyring" was Aura-path-scoped). The Makefile needed no changes (`GEN_DIR`/`OPENAPI_DIR` were already repointed to `packages/shared` in the `aura-client` task). Outcome: the scripts' only remaining MCP usage is the Atlassian Teamwork Graph; the `aura-access-rewrite` map is complete — the MCP wrapper is fully out of the Aura scripts path.

## 2026-08-20 — Migrate aura.ts/aura-digest.ts to AuraClient; dedupe types.ts (call-site-migration)
Migrated `scripts/src/aura-digest.ts` + `scripts/src/aura.ts` from `McpClient.callTool` to the typed `AuraClient` interface via `createDefaultAuraClient()` (all 21 exercised Aura verbs). Dropped `REQUIRED_TOOLS`/`assertToolsAvailable`/all Aura `client.close()` calls; kept the Atlassian `McpClient` path in `devlinks.ts` (out of scope). Added `dbus-next` to the esbuild `external` array (transitive keyring dep with an optional `x11` require). Removed 20 hand-maintained Aura-response shapes from `scripts/src/types.ts` (now single-sourced in `@pi-aura/shared/aura-client`); retyped embedded references (`RawAuraData`, `DigestReview.decisions`, `ArtifactVerification.current`, `fetchTaskDevLinks`) to domain types; kept the digest/report/diff/dev-link types. Field renames: Artifact `current_version`→`latest_version`, KnowledgeNode `uuid`→`id`/`version`→`latest_version`, UploadDocument pages `text`→`content`. Outcome: the scripts' Aura path is MCP-wrapper-free and the types are single-source — `clients-cleanup` can now remove `bearerClient`'s Aura path + the dead `scripts/src/generated/` tree + `scripts`' `@hey-api/*` deps.

## 2026-08-20 — AuraClient interface + HeyApiAuraClient impl + createDefaultAuraClient() factory (aura-client)
Moved the `@hey-api` REST codegen from `scripts/` into `packages/shared/` (single-source generated client, `@hey-api/client-fetch` runtime dep, `make codegen` target repointed). Added an implementation-agnostic `AuraClient` interface (21 exercised verbs) with expressive domain types in `@pi-aura/shared/aura-client` — no generated types leak into the interface (Q8). Implemented `HeyApiAuraClient` (delegates to the generated SDK, maps domain<->generated internally, `Authorization: Bearer <PAT>` from the keyring, throws `AuraApiError` on SDK errors) + `createDefaultAuraClient()` factory (reads `aura.baseUrl` from settings.json, builds the keyring, validates the PAT). 4 `tsx --test` smoke tests pass; `scripts/` untouched (rewiring is the next task). Outcome: the shared `AuraClient` layer is ready for `call-site-migration` to drop the MCP wrapper from the scripts path.

## 2026-08-20 — Map complete: Rewrite the Aura access layer to drop the MCP wrapper (aura-access-rewrite)
All 8 child tasks done: two grillings (access-layer + keyring-interface design), workspaces bootstrap, from-scratch keyring rewrite (pure-JS dbus-next Secret Service), the `@pi-aura/shared` `AuraClient` interface + `HeyApiAuraClient` + `createDefaultAuraClient()` factory, `/aura secrets` slash-command (discover + edit), call-site migration of `aura.ts`/`aura-digest.ts` to `AuraClient` + `types.ts` dedupe, and `clients-cleanup` removing the dead `bearerClient`/generated tree/`@hey-api` deps. Outcome: the pi-aura scripts talk to Aura directly via the generated REST client behind an implementation-agnostic `AuraClient` (PAT from the OS keyring, base URL from settings.json); the MCP-over-HTTP wrapper is gone from the Aura path (the `aura-mcp-dev` MCP entry stays for agent-driven work via the `aura` skill); the only remaining MCP usage in scripts is the Atlassian Teamwork Graph.

## 2026-08-20 — aura-digest: since-last-run + older-unread notifications, drop auto mark-read (digest-notif-window)
Changed `DigestAttention.notifications` from `string[]` to `DigestNotifications { since_last_run: string[]; older_unread: string[] }`. `fetch` now reads `~/.pi/aura/last-digest.json` `fetched_at`, paginates `listNotifications` newest→oldest (page size 50, 500-item hard cap with a `warnings[]` entry on overflow), and splits at `fetched_at − 5min`: everything newer (read + unread) → `since_last_run`, newest 20 at/older with read dropped → `older_unread`. `summarizeNotifications` lost its internal unread filter (moved to the caller); `extractVerifyTargets` and the `review_assigned` filter receive the combined list so review signals aren't lost. `renderAttention` renders two lines with empty-state text. `skills/aura-digest/SKILL.md` Step 4 no longer instructs `markAllNotificationsRead()`; a note documents that the digest does not mutate read state. Accepted the persisted-shape break (no `schema_version` bump — the store regenerates each `save`). Outcome: the digest now shows everything that landed since the last pull plus a bounded older-unread reminder, and never silently marks notifications read.

## 2026-08-24 — First-pass pi-adaptation of the 14 engineering-foundation blueprint skills (adapt-blueprint-skills)
The seed (commit 7def2b1, "adapt on first seed too — no verbatim copies kept") had already adapted all 14 skills in place at resources/blueprint/skills/ — so the task became (1) fixing the 8 residual Cursor tool-call edges the seed missed (task-refine: 6 "Q&A module"→ask_user_question + 1 editor-context example; task-implement: 2 "Editor Window/Cursor window"→"their editor"), and (2) moving the 14 SKILL.md + 4 task-untangle .ts companions to the design-Q6 top-level layout skills/engineering-workflow/<name>/ with the drift manifest's 18 localPath entries, blueprintPathToLocal, the engineering-workflow router, and the engineering-sync skill's path table all rewired to the new locations. Outcome: the 14 blueprint skills are now invokable pi sub-skills discovered recursively under skills/engineering-workflow/, the sync manifest points at the new homes so future fetch runs reconcile in place, and the Cursor-edge surface across all 14 is zero. Recorded two workflow frictions: the implement-task chain syntax vs. the workflowScript API mismatch (false-negative on slice 1), and node --experimental-strip-types can't run the sync test (use tsx; documented in docs/testing.md).

## 2026-08-24 — Map complete: Mirror the engineering-foundation wiki space into pi-aura (engineering-foundation-sync)
All 8 child tasks done: blueprint-skills-and-sync-design (the two grillings settling Q1–Q14), cursor-rules-incorporation (the 15 .mdc rules + engineering-rules.ts extension), move-skills-to-core (skills/aura* → skills/core/aura* + 4 rippling path refs), engineering-workflow-skill (the user-facing canon router + resources skeleton), engineering-sync-skill (the package-author-only sync skill + fetch/finish/status/mv CLI utility + three-way reconciliation + drift gate), seed-engineering-mirror (the first fetch seeding the 43-file mirror + .pi/engineering-foundation.json, with the v2 user-review-gated flow), adapt-blueprint-skills (edge-fix + relocate to design-Q6 top-level layout after the seed overtook the from-scratch adaptation), and engineering-rules-extension (frontmatter dispatch + universal @mention + system-prompt listing). Deviation: the seed's adapt-in-place flow (commit 7def2b1) superseded the map's "verbatim source + adapted derivative" decision (Q5) — there are no verbatim copies on disk; the manifest records adaptedSha256 directly against the wiki sourceSha256. Outcome: the engineering-foundation canon is mirrored locally, surfaced by the engineering-workflow pi skill, kept fresh by the package-author-only engineering-sync skill, with the 14 blueprint skills invokable as pi sub-skills at skills/engineering-workflow/<name>/ and the 15 Cursor rules active in pi via the engineering-rules extension.
