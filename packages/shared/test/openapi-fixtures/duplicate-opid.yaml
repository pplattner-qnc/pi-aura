openapi: 3.1.0
info:
  title: Duplicate operationId fixture
  version: 0.0.1
paths:
  /dup:
    get:
      operationId: dupId
      summary: first
      responses:
        "200":
          description: ok
  /dup2:
    post:
      operationId: dupId
      summary: second
      responses:
        "200":
          description: ok
components:
  schemas: {}
